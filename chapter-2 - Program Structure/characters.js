// Write a program that counts the number of characters in a given string
const count_characters = function (word) {
    return word.length;
}

let sentence = "Now i'am become death the destroyer of Worlds!";
let result = count_characters(sentence);
console.log(result)
