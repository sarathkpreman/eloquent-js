// Q2.1: Write a function that takes a first name and last name as arguments and returns the full name.
const i_am = (first_name, last_name) => {
    return `${first_name} ${last_name}`;
}

// Q2.2: Given a string "Learning JavaScript", write code to:
// Extract "JavaScript" using a method.
// Convert the string to all uppercase.

const extract = (str) => {
    let extracted;
    return extracted = str.substring(9, 19)
}

// Q2.3: Create a function that accepts a string and returns the number of characters in it. Try it with an empty string as well.
const no_of_characters = (string) => {
    let len;
    return len = string.length;
}

