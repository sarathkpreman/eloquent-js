// Q1.1: Write a program to calculate the area of a rectangle with width 10 and height 5.
const area = (width, height) => {
    return width * height;
}

// Q1.2: Create a function that accepts a number and returns true if the number is even and false if it’s odd. Use the modulo operator.
const even_or_not = (number) => {
    return number % 2 == 0 ? true : false;
}

// Q1.3: Solve this expression manually and confirm it using JavaScript:
console.log(10 + 2 * 5 - 3 / 3);

