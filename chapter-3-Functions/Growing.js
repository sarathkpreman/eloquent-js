const iamGrowing = function(n) {
    let grow = [];
    for(let i = 0; i<=n; i++){
        grow.push(i)
        return grow
    }
    return grow
}

const recived = iamGrowing(6);
console.log(recived)